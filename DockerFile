# 1. Start with an official Node.js image
FROM node:22

# 2. Set a working directory inside the container
WORKDIR /app

# 3. Copy your package.json and install dependencies
COPY package*.json ./
RUN npm install

# 4. Copy the rest of your app's code
COPY . .

# 5. (Optional) Run Truffle commands
# RUN npm install -g truffle
# RUN truffle compile

# 6. Tell Docker what command to run when the app starts
CMD [ "npm", "start" ]